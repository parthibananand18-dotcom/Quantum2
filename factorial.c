#include<stdio.h>
int main()
{
  int n,i=1,fact=1;
  printf("Enter the value of n: ");
  scanf("%d",&n);
  while(i<=1)
    {
      Fact=fact*i;
      i++;
    }
  printf("Factorial of natural numbers: %d",Fact);
  return 0;
}
